app_name: "EXAM"
version: "1.0.0"

log_leve: "INFO"

settings:
  max_attempts: 5
  timeout: 30
  debug: false

modules:
  - name: "auth"
    enabled: true
  - name: "database"
    enabled: false
  - name: "api"
    enabled: true